node_modules
.wrangler
# `**/` so tests in src/ are excluded too — a bare `*.test.ts` only matches the
# context root, leaving test files (and their fixtures) shipping in the image.
**/*.test.ts
.dev.vars
.dev.vars.example
