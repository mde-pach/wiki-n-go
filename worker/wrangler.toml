name = "wiki-n-go"
main = "src/index.ts"
compatibility_date = "2026-06-01"

# Secrets (set with `wrangler secret put`): GITHUB_TOKEN, HASH_SECRET, and — to
# enable optional GitHub sign-in — OAUTH_CLIENT_SECRET + SESSION_SECRET.
[vars]
REPO_OWNER = "mde-pach"
REPO_NAME = "wiki-n-go"
BRANCH = "main"
CONTENT_DIR = "content"
HOME_SLUG = "index"
ALLOWED_ORIGIN = "https://mde-pach.github.io,http://localhost:4321"
# Talk topics post to this Discussions category; its node id (and the repo id)
# are derived at runtime, so a fork only names the category here.
DISCUSSION_CATEGORY = "General"
# Optional GitHub sign-in. Set OAUTH_CLIENT_ID to your OAuth App's client id
# (public); leave commented to keep sign-in disabled. The matching secrets are
# OAUTH_CLIENT_SECRET and SESSION_SECRET (a long random string for signing
# session tokens). See README "Optional: GitHub sign-in".
# OAUTH_CLIENT_ID = "Iv1.xxxxxxxxxxxxxxxx"
# Autonomous editing. DEFAULT_EDIT_TIER = "maintainer" keeps the reviewed-PR
# model for any page without a `protection:` frontmatter field (set "open" to
# flip the default to immediate-publish). Trust thresholds match Wikipedia's.
DEFAULT_EDIT_TIER = "maintainer"
AUTOCONFIRM_EDITS = "10"
AUTOCONFIRM_DAYS = "4"
EXTENDED_EDITS = "500"
EXTENDED_DAYS = "30"

[[kv_namespaces]]
binding = "RATE_LIMIT"
id = "ba108356468b483286ff7fecf452cbf5"
