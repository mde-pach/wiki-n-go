{
  "name": "wiki-n-go-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "verify": "tsc --noEmit && vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "typescript": "^5",
    "vitest": "^4.1.8",
    "wrangler": "^4"
  },
  "dependencies": {
    "@openauthjs/openauth": "^0.4.3",
    "valibot": "^1.4.1",
    "yaml": "^2.9.0"
  }
}
