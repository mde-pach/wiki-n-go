{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in *.ts|*.tsx|*.css|*.json|*.mjs|*.js) cd \"${CLAUDE_PROJECT_DIR:-.}\" && bunx biome check --write \"$f\" >/dev/null 2>&1 || true ;; esac; }"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PROJECT_DIR:-.}/.claude/hooks/verify-on-stop.sh\""
          }
        ]
      }
    ]
  }
}
