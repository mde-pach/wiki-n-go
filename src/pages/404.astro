---
import Discussion from "../components/Discussion.tsx";
import Editor from "../components/Editor.tsx";
import WikiPage from "../components/WikiPage.tsx";
import Base from "../layouts/Base.astro";

// Renders any page added after the last deploy; slug comes from the URL.
---

<Base title="Wiki">
  <WikiPage client:only="solid-js" />
  <Editor client:only="solid-js" />
  <Discussion client:only="solid-js" />
</Base>
